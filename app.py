"""
app.py — Streamlit dashboard (Phase 8).

For now this is just a placeholder so the project structure is complete.
We will build the real UI after ingestion and extraction work.
"""

import streamlit as st

st.set_page_config(page_title="Clinical Document Hub", page_icon="🏥")
st.title("Clinical Document Intelligence Hub")
st.info(
    "Phase 0 complete — project skeleton is ready. "
    "Next step: document ingestion (Phase 1)."
)
