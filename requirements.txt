# Core (assignment stack)
python-dotenv>=1.0.0
pydantic>=2.0.0
openai>=1.0.0  # Groq-compatible client (base_url set in config.py)
streamlit>=1.28.0

# Document reading (Phase 1+)
pymupdf>=1.24.0
pytesseract>=0.3.10
Pillow>=10.0.0

# Testing (later phases)
pytest>=8.0.0
